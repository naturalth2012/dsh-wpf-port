<Window x:Class="Dsh.Wpf.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource Surface1}" Foreground="{DynamicResource TextPrimary}" Width="380">
    <StackPanel Margin="16">
        <TextBlock x:Name="PromptText" Margin="0,0,0,4" TextWrapping="Wrap"/>
        <TextBox x:Name="InputBox" Margin="0,0,0,12" Padding="6" MinHeight="60"
                 AcceptsReturn="True" TextWrapping="Wrap"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="确定" Click="Ok_Click" IsDefault="True" Padding="16,5" Margin="0,0,8,0"/>
            <Button Content="取消" Click="Cancel_Click" IsCancel="True" Padding="16,5"/>
        </StackPanel>
    </StackPanel>
</Window>
